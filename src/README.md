# Steps followed for uv setup

uv run django-admin startproject backend .

## Django specific commands

### Start the server
uv run manage.py runserver

### Add an application
uv run manage.py startapp bingo